import React from 'react';
import ReactDOM from 'react-dom';
import ProgressBar0 from './ProgressBar0';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<ProgressBar0 />, div);
});
